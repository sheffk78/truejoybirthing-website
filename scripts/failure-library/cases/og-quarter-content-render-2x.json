{
 "id": "og-quarter-content-render-2x",
 "date": "2026-09-10",
 "stage": "build",
 "city": "hayward-ca",
 "failure": "OG city images shipped with real content occupying only the top-left 50%x50% of the 1200x630 canvas (hayward-ca, corona-ca; hayward reported by Kenneth 2026-09-10). Root cause: scripts/render-og-2x.cjs screenshots a 2400x1260 viewport at deviceScaleFactor 2 then downsizes to 1200x630 — but compositions are 1200px CSS wide, so the rendered card occupies only the top-left quarter of the screenshot and the downsize keeps the quarter-geometry. S2 (file exists >=30KB) passed; nothing measured content coverage. Sibling defect same ship: san-diego-ca OG was 1344x768 (older design, wrong dims, never checked). Also in same ship: hayward birth-centerDetails entry lacked 'paragraph', rendering an empty text card (validate-city-data.ts checkFacilityParas added 2026-09-04, two weeks AFTER hayward shipped, so the gap predates the check).",
 "detector": "check-og-content.py (S11): dims==1200x630, content bbox >=92% of width AND height vs corner-median background, density >=30%. Negative-tested: old hayward file FAILs (50%/50%/11%), fixed file passes. Birth-center paragraph gap caught by existing checkFacilityParas (validated by synthetic removal -> validator exit 1).",
 "replay": {
  "type": "og_content_coverage",
  "expect": "fail",
  "fixture": "test-fixtures/og-defects-2026-09-10/og-city-hayward-ca-quarter.webp (copied 2026-09-11 from public/images/og-city-hayward-ca.webp — the unreferenced pre-fix base file still on disk; CDN also still serves it)",
  "command": "python3 scripts/check-og-content.py --file test-fixtures/og-defects-2026-09-10/og-city-hayward-ca-quarter.webp",
  "expect_output": "FAIL (file) og-city-hayward-ca-quarter.webp: content covers 50% of width; content covers 50% of height; density 11% < 30%"
 },
 "sweep": {
  "type": "og_content_coverage",
  "expect": "pass",
  "command": "python3 scripts/check-og-live-sweep.py [hayward-ca]",
  "expect_output": "S11-live: 1/1 pass",
  "note": "Live sweep verifies the LIVE og:image URL. On 2026-09-10 the ogImage ref was swapped to the -v2 file; the base files for hayward+corona remain quarter-content ON DISK and on the CDN (verified 2026-09-11). The live sweep guards the referenced URL, not orphan files."
 },
 "source_commit": "6a80c350..HEAD",
 "status": "active"
}